<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="StarAIAgentUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StarAIAgentUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AI Agent">
    <Grid x:Name="RootGrid" Background="Transparent">
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>-->
        <!--<Grid x:Name="MainAppContent" Grid.Row="1" Background="Transparent">
            <Grid x:Name="ContentPanel" Background="Transparent">
                --><!--<Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="LeftColumn" Width="120" MinWidth="50" />
                    <ColumnDefinition Width="*" MinWidth="300" />
                </Grid.ColumnDefinitions>

                <Grid x:Name="SidebarPanel" Grid.Column="0" Background="#10000000" Padding="12" RowDefinitions="Auto,*">
                    <Button Content="+ New Task" Background="#8B5CF6" Foreground="White" HorizontalAlignment="Stretch" Padding="12,8" CornerRadius="8" BorderThickness="0"/>
                    <ListView x:Name="TaskList" Grid.Row="1" Margin="0,12,0,0" Background="Transparent" BorderThickness="0"/>
                </Grid>--><!--

                
            </Grid>
        </Grid>-->
        <Grid x:Name="ChatWorkspacePanel" Grid.Column="1" Padding="16" RowDefinitions="*,Auto,Auto" RowSpacing="12" Background="#05FFFFFF">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ChatPanel" Spacing="16" Padding="4"/>
            </ScrollViewer>
            <TextBox x:Name="InputBox" Grid.Row="1" PlaceholderText="Ask something..." CornerRadius="8" Padding="12,10" BorderThickness="1" BorderBrush="#4C8B5CF6" PreviewKeyDown="InputBox_PreviewKeyDown" TextWrapping="Wrap" AcceptsReturn="True"/>
            <Button x:Name="SendButton" Content="Send" Grid.Row="2" Click="Send_Click" Background="#7C3AED" Foreground="White" HorizontalAlignment="Left" Padding="24,6" CornerRadius="8" BorderThickness="0"/>
        </Grid>
    </Grid>
</Window>